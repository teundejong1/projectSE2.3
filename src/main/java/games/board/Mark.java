package games.board;

/**
 * @author Jeroen Lammersma
 * Emum for Marks
 * Empty means the cell in the board is current not set
 * ONE means the cell contains a mark for player one
 * TWO means the cell contains a mark for player two
 */
public enum Mark {
    EMPTY,
    ONE,
    TWO
}
